Your hook scripts go here (Labs C1–C4), and the `hooks` block that registers
them goes in `.claude/settings.json`.

On macOS and Linux remember `chmod +x` on every script, or the hook fails
instead of firing.

Reference answers live in `solutions/hooks/` and `solutions/settings/`.
