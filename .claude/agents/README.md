Your custom subagents go here (Labs A1–A3).

One Markdown file per subagent. `name` and `description` are the only required
frontmatter fields. The file name does not have to match `name`.

Reference answers live in `solutions/agents/`.
